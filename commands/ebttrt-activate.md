---
name: ebttrt-activate
description: Activate ebttrt on this Mac (plugin, rule, CLI, PATH, Hoheit)
---

Load skill `ebttrt-activate`. Run `ebttrt activate` (or `python3 ~/Developer/ebttrt/scripts/ebttrt.py activate` if the CLI is not on PATH). Then `ebttrt doctor`.
