---
name: ebttrt
description: Start the ebttrt loop and run prove before done
---

$ARGUMENTS

Load skill `ebttrt`. If arguments are present, `ebttrt consult` then `ebttrt begin` with that goal and follow the pipeline through `ebttrt prove --record` and `ebttrt done`. Never skip prove.
